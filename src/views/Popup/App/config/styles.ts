import { CSSProperties } from 'react'

export default <Record<string, CSSProperties>>{
  field: {
    display: 'flex',
    alignItems: 'center',
  },
  icon: {
    marginLeft: '12px',
  },
}
